//test 1
if (NaN) text = "Hey dummy, that's not a number!";

//test 2
age = Number(age);
if (isNaN(age)) {
    voteable = "Input is not a number";
} else {
    voteable = (age < 18) ? "Too young" : "Old Enough";
}

//test 3
if (NaN = userInput) {
    message = "Hey dummy, that's not a number!";
  }